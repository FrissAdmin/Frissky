query($token: String!) {
  queryRoleCustomer(token: $token) {
    surveyAnswers {
      answer
      choice: choiceId
      question: questionId
    }
  }

  surveyQuestions {
    choices {
      id
      title
    }
    id
    subtitle
    title
    type
  }
}
